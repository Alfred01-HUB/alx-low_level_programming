#include "main.h"
/**
 * abs - computes the absolute value of an integer
 * @n: the integer in the question
 * Return: the abs if the int.
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}

