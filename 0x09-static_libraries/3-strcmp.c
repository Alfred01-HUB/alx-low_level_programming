#include "main.h"
#include <string.h>
/**
 * _strcmp - concatenate two strings
 * @s2:character to be compared
 * @s1:character to be compared
 * Return: 0
 */
int _strcmp(char *s1, char *s2)
{
	int cmp;

	cmp = strcmp(s1, s2);
	return (cmp);
}

