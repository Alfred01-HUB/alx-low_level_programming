#include "main.h"
/**
 * _isupper - print which is upper or lower case
 * @c:param
 * Return: 0 and 1
 */
int _isupper(int c)
{
	int a;

	for (a = 97; a <= 122; a++)
	{
		if ((c >= a) && (c >= a))
		return (0);
		else
		return (1);
	}
}

